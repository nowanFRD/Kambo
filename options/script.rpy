default persistent.playground_1 = True

default persistent.player = None

default persistent.flinn = None

default persistent.eileen = None

default persistent.playground_2 = False

default persistent.lang = True

init python:

    config.developer = True

    config.rollback_enabled = False

    config.has_autosave = False

    config.has_quicksave = False

    config.has_sync = False

    config.has_voice = False